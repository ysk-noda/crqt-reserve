<!-- DECISION LOG FORMAT RULES
このファイルはプロジェクトの意思決定ログです。
目的：同じ議論を繰り返さないために、議論の流れが読み取れるように極力詳しく書くこと。

各エントリは以下のフォーマットで記録する：

## [YYYY-MM-DD] テーマ

### 背景・前提
なぜこの議論が始まったか。前回からの積み残しがあればその内容。

### 試したこと・起きたこと
何を試して何が起きたか。失敗も含めて具体的に残す。
「〇〇を試したが××という理由でダメだった」まで書く。

### 変わった認識
この議論を経て「前はこう思っていたが今はこう思っている」という認識の変化。
ここが一番重要。同じ話をしないために必要な部分。

### 決めたこと
具体的なアクションと理由。「なぜそうするか」も必ず添える。

### やらないと決めたこと・理由
同じくらい重要。「なぜやらないか」を残す。

### 次の一手
直近でやること。1〜3個に絞る。

### 保留・積み残し
-->

---
# 議事録・判断ログ

<!-- 会話の終わりに「今日の議論をdecision_log.mdの形式でまとめて」と投げると自動で埋めてくれる -->
<!-- 新しい記録は上に追記していく -->

---

## [2026-08-06] 予約キャンセルの導線を修正

### 背景・前提
利用者から「キャンセルの仕方が分からない」と連絡があった。
その方は**メールアドレスを入力して予約していた**ため、既知の「メール未入力だとキャンセル不可」の話ではなかった。

### 試したこと・分かったこと
- `send-booking-email` の本文を読んだところ、**リンクの飛び先がトップページのURLそのもの**だった

  ```ts
  const cancelUrl = appUrl ?? 'https://crqt-reserve.vercel.app'
  ...
  <p>予約内容の確認・キャンセルはこちら</p>
  <a href="${cancelUrl}">予約のキャンセルはこちらから</a>
  ```

  → 「キャンセルはこちらから」を押すと**新規予約の画面（施設選択）が開くだけ**。
  そこから右上の「予約確認」を自分で見つけ、メールアドレスを再入力する必要があった
- ヘッダーのボタンが「**予約確認**」で、キャンセルもできることが読み取れなかった

### 決めたこと・理由
- **確認メールのリンクに `?my=1&email=...` を付ける**。
  開いた瞬間に予約確認モーダルが開き、メールアドレスも入った状態で予約一覧が出る（クリック1回でキャンセルボタンまで到達）
- **ヘッダーのボタンを「予約確認・キャンセル」に変更**。狭い画面では「確認・取消」に切り替え
  （長いラベルがヘッダーで折り返すのを防ぐため）
- モーダルの見出しは元々「予約確認・キャンセル」だったので、ボタンと表記が揃った

### やらないと決めたこと・理由
- **メールアドレスの必須化はしない**。2026-03-26に「任意入力のまま運用」と決定済み。
  今回改めて提案が出たが、**その決定を読まずに出した提案だった**ため取り下げ
  （メール未入力の予約は本人キャンセル不可。スタッフが `/admin` から取り消す運用を継続）

### ドキュメントの誤りを修正
- `docs/system-overview.md` に「時間選択グリッド 09:00〜18:00」と書かれていたが、
  2026-06のコミット `e1aa7b3` で **09:00〜24:00 に拡張済み**だった（夜間会員対応）。
  仕様書が更新されておらず、これを信じて会員ガイドに誤った時間を記載してしまった
- → 仕様書を実装に合わせて修正した
- **教訓：動作に関わる数値（時間・金額・上限）はコードで確認する。仕様書は設計意図を読むために使う**

### デプロイ手順（この改修で確認できたこと）
- **フロント**：main へ push すると Vercel が自動デプロイ
- **Edge Function**：git push では反映されない。別途デプロイが必要

  ```bash
  npx supabase login          # 別途PowerShellを開いて実行（Claude Code内蔵ターミナルは入力を受け付けない）
  npx supabase functions deploy send-booking-email --project-ref jldhnoohjtwhzavjqgub
  ```

- ビルド検証は**ローカル一時フォルダにコピーしてから**行う（Drive上での `npm install` は失敗する。2026-07-09の記録どおり）

### 保留・積み残し
- メール未入力の予約はキャンセル不可（継続）
- 予約完了画面（Step3）にキャンセル方法の案内を出す案は未実施

---

## [2026-07-09] 管理者画面（/admin）の一覧改修

### 背景・前提
- `/admin` の予約一覧が「全期間表示・メール非表示・検索なし」で、当日の状況確認や過去反響顧客への追客に使いづらかった。
- `bookings` テーブルに `email` 列は既に存在（`001_add_email.sql` で追加済み）していたが、管理画面では列として出していなかった。

### 試したこと・起きたこと
- Google Drive（G:）上のリポジトリで `npm install` すると `caniuse-lite` 等で tar の ENOENT / EBADF エラーが出てビルド検証ができなかった。→ ソースをローカル一時フォルダにコピーして `npm install && npm run build` で検証成功（88 modules, ビルドOK）。以後 Drive 上での npm 実行は避ける。
- 当リポジトリに git identity（user.name/email）が未設定でコミットが `fatal: unable to auto-detect email address` で失敗。→ このリポジトリにローカル設定（`ysk-noda` / `noda@kawata.org`）して解消。
- コミットメッセージを PowerShell の here-string 記法（`@'...'@`）で書いたが Bash ツールで実行したため、先頭に余計な `@` 行が入った。→ `git commit --amend -F -` のヒアドキュメントで修正。以後 Bash では here-string でなくヒアドキュメントを使う。

### 変わった認識
- 検索欄の適用範囲について。当初は「今週デフォルト」の枠内で検索する案も考えられたが、**この管理画面の検索は追客（過去反響顧客を探す）用途が主**と判断。検索時は今週フィルタを外して全期間を横断する仕様に決めた。

### 決めたこと・理由
- **メールアドレス列を追加**（お名前の隣、`mailto:` リンク付き。未入力は「—」）。管理者がその場で連絡できるようにするため。
- **デフォルト表示を「今週（月〜日）」に絞り込み**。「今週／全期間」の切替ボタンを設置。普段は直近の予約だけ見たいニーズが大半のため。週範囲は `getWeekRange()` で月曜起点に算出。
- **名前・メールの検索欄を追加**。部分一致・大文字小文字無視（Supabase `ilike` の `.or()`）、入力300msのデバウンスでDB問い合わせ。検索時は期間フィルタを外し全期間から探す。
- 検索中・日付指定中は「今週／全期間」ボタンを無効化（意図しない組み合わせを防ぐ）。施設フィルタは検索と併用可。

### やらないと決めたこと・理由
- 検索を「今週内だけ」に限定するのは却下。過去の予約者を探せず、追客という主目的に使えなくなるため。
- 管理者パスワードの平文比較（Supabase Auth 移行）は今回スコープ外。既存の積み残しのまま。

### 次の一手
- 本番（Vercel 自動デプロイ）反映後、`/admin` で実データの表示・検索・今週絞り込みを目視確認。
- 追客運用で「CSV出力」が欲しくなったら別途対応（改善候補に既出）。

### 保留・積み残し
- メール未入力の予約はキャンセル不可（既存の積み残し、継続）。
- 管理者パスワードのフロント平文比較（Supabase Auth 移行推奨、継続）。

---

## [2026-03-26] 利用規約・extend-bugfix・UI細部修正

### やろうとしたこと
利用規約ページの追加、extend-bookingのHTMLバグ根本解決、時間選択UXの修正、welcomeページのボタン文言修正。

### 検討した選択肢

| 案 | メリット | デメリット | 判断 |
|---|---|---|---|
| 利用規約リンクをページ下部フッターに置く | 一般的な配置 | PCでは2カラムレイアウトが画面に収まりスクロールが発生しないため見えない | 却下 |
| 利用規約リンクをstickyヘッダー内に置く | PC・スマホ両方で常時表示 | — | 採用 |
| extend-bookingでHTMLを返す | シンプル | Supabaseゲートウェイが Content-Type を text/plain に強制上書き（変更不可） | 却下 |
| extend-bookingを302リダイレクト方式に | ゲートウェイ制約を回避 | React側に結果ページ（/extend-result）が必要 | 採用 |
| 時間選択をINCLUSIVE方式に | 「最後のスロットをクリック」で直感的 | 30分予約不可・同スロット再クリックでキャンセルできない問題が発生 | 却下 |
| 時間選択をEXCLUSIVE方式に戻す | 全パターン正常動作 | 終了時刻スロットが視覚的に分かりにくい | 採用（終了スロットを薄青でハイライト補完） |

### 決めたこと・理由

**利用規約ページ（/terms）**
- 取得情報・利用目的・禁止事項・免責事項・準拠法を記載
- リンクはstickyヘッダー内（「三島クロケット」テキスト横）に配置
- BookingPage（/・/drop-in共通）とWelcomePage（/welcome）に追加

**extend-booking バグ根本解決**
- curl調査でSupabaseゲートウェイが `Content-Type: text/plain` + `X-Content-Type-Options: nosniff` を強制付与することが判明（コード側では対処不可）
- 302リダイレクト方式に切り替え、結果表示は `/extend-result`（React）で行う
- 延長の2時間上限チェックを撤廃し、後続予約の競合チェックのみで判定

**時間選択UI確定**
- EXCLUSIVE方式（2クリック目=終了時刻）を正式採用
- 終了時刻スロットを薄青（bg-blue-200）でハイライトし選択範囲の終端を明示

**welcomeページ**
- ボタン文言「月額会員の方」→「会員の方」に変更
- ボタン内テキストを中央寄せに修正

**システムドキュメント**
- `docs/system-overview.md` を新規作成（全体像・技術スタック・DB構造・連携フロー等を網羅）

### 次の一手
- `/welcome` URLをQRコードにして施設内に掲示
- 会員・ドロップイン双方に運用開始を周知
- アラートメール・延長機能の実運用フィードバック収集

### 保留・積み残し
- メール未入力の予約はキャンセル不可（要改善）
- 管理者パスワードがフロントエンドで平文比較（Supabase Auth移行推奨）
- 繰り返し予約・CSV出力などの追加機能候補あり
- メールアドレスは引き続き任意入力のまま運用。未入力で確認メールが届かない事例が発生しているが、必須化は見送り。不都合が続くようなら再検討する

---

## [2026-03-24] UI改善・バグ修正・機能追加まとめ

### やろうとしたこと
運用開始後のフィードバックをもとに、UX改善・バグ修正・新機能追加を一括対応する。

### 検討した選択肢

| 案 | メリット | デメリット | 判断 |
|---|---|---|---|
| 時間選択をINCLUSIVE方式に | 「最後のスロットをクリック」で直感的 | 30分予約不可・同クリックでキャンセルできない問題が発生 | 却下 |
| 時間選択をEXCLUSIVE方式に戻す | 30分〜2時間の全パターンが正しく動作 | 終了時刻の視覚的フィードバックが弱い | 採用（終了スロットを薄青でハイライト補完） |
| extend-booking でHTMLを返す | シンプルな実装 | Supabaseゲートウェイが Content-Type を強制 text/plain 上書き → HTML がソース表示される | 却下 |
| extend-booking を302リダイレクト方式に | ゲートウェイの制約を回避できる | React側に結果ページが必要 | 採用（/extend-result を新設） |
| /drop-in を独立コンポーネントにする | 分離が明確 | BookingPage の重複が増える | 却下 |
| BookingPage に facilities prop を渡す | コード共通化・保守しやすい | — | 採用 |

### 決めたこと・理由

**時間選択UI**
- EXCLUSIVE方式（2クリック目=終了時刻）を正式採用
- 終了時刻スロットを薄い青（bg-blue-200）でハイライトし、選択範囲の終端が視覚的にわかるよう改善

**extend-booking バグ修正**
- Supabase ゲートウェイがセキュリティポリシーで Content-Type: text/html → text/plain に強制上書きすることが判明
- 対策：EdgeFunction から HTML を返すのをやめ、302リダイレクトで `/extend-result`（React）に結果を渡す方式に変更
- 延長の2時間上限チェックを撤廃し、後続予約の競合チェックのみで判定

**URL別施設表示**
- `/`：会員向け（全施設：会議室2室＋フォンボックス3つ）
- `/drop-in`：ドロップイン向け（フォンボックス3つのみ）
- `/welcome`：QRコード用の入口ページ（会員/ドロップインを選択）
- BookingPage に `facilities` prop を渡すだけで共通化を実現

**名前・メールの自動入力**
- 予約完了時に `crqt_name` / `crqt_email` をlocalStorageに保存
- 次回アクセス時のStep2で自動入力される（/drop-inでも共通動作）

**その他**
- 送信元メールアドレスをすべて `info@crqt.work` に統一
- PC版レイアウト改善（施設タブを横並びで広く表示）
- システム概要ドキュメントを `docs/system-overview.md` として新規作成

### 次の一手
- 月額会員へ `/welcome` URLをQRコードで配布
- ドロップイン利用者の動線をQRコード（/welcome）で案内
- アラートメール・延長機能の実運用フィードバック収集

### 保留・積み残し
- メール未入力の予約はキャンセル不可（要改善）
- 管理者パスワードがフロントエンドで平文比較（Supabase Auth移行推奨）
- 繰り返し予約・CSV出力などの追加機能候補あり

---

## [YYYY-MM-DD] テーマタイトル

### やろうとしたこと

### 検討した選択肢
| 案 | メリット | デメリット | 判断 |
|---|---|---|---|
|   |   |   |   |

### 決めたこと・理由

### 次の一手

### 保留・積み残し

[2026-03-18] 施設予約システム構築・AI活用社内展開準備
やろうとしたこと
三島クロケットの会議室・フォンボックス予約システムを構築し、非エンジニアによるAI活用の実績として社内共有できる形にまとめる。
検討した選択肢
案メリットデメリット判断GASで実装既存GWSで完結複数人同時予約の重複リスク・スマホUI困難却下Webアプリ（Supabase+Vercel）UIの自由度・同時接続の安定性新規ツールの習得コスト採用ログイン認証ありセキュリティ向上企業契約で登録者と利用者が異なる問題却下URL共有制（認証なし）運用シンプル・全員使えるURLが漏れると誰でも予約可能採用（実態に合う）LINE連携利便性向上LINE未登録会員がいる・実装複雑見送り（Webアプリ優先）
決めたこと・理由

技術構成：React+Vite / Supabase / Vercel / Resend / GitHub
認証：ログインなし・URL共有制（企業契約の実態に合わせる）
メール識別：メールアドレスを識別子としたマイページ機能
自動処理：pg_cron+pg_netで終了5分前アラートメール・延長機能
送信元：info@crqt.work（Cloudflare移行後にResendドメイン認証完了）

次の一手

月額会員へのアプリURL共有
動作確認しながらフィードバック収集
社内メンバーへのAI活用共有（スライド7枚作成済み）
社内AI活用の土壌づくり（まずClaudeのシンパを増やす）
GWS契約のGemini Proを慣れる入口として活用検討

保留・積み残し

メール未入力の予約はキャンセル不可（要改善）
管理者パスワードがフロントエンドで平文比較（Supabase Auth移行推奨）
繰り返し予約・CSV出力などの追加機能候補あり
全員が自分でAIツールを使う体制は費用面から現時点では強制しづらい